{
  "mission": "{{MISSION_NAME}}",
  "rules": "Agents may ONLY flip 'passes' from false to true, after verifying every step. Editing or removing descriptions/steps is unacceptable and will be treated as a failed task. Keep the 'bootstrap' field on every feature: false (default) means a worker implements it and verify-mission requires a worker receipt; true means no worker will ever run it (orchestrator-authored docs, AGENTS.md, mission scaffolding). Set it when authoring — adding it after GATE 0 is an edit to an approved feature.",
  "features": [
    {
      "id": "F001",
      "category": "functional",
      "priority": 1,
      "bootstrap": false,
      "description": "{{User-visible behavior, e.g.: Fund Detail page renders Overview tab with fund name, category badge, and ISC figures fetched from GET /finduck/funds/:id}}",
      "steps": [
        "{{Navigate to /funds/<known-id> while logged in}}",
        "{{Verify fund name and COVIP category render}}",
        "{{Verify ISC 10yr and 35yr figures match the DB row}}",
        "{{Verify no direct Supabase call from the browser (network tab: only Worker origin)}}"
      ],
      "passes": false
    },
    {
      "id": "F002",
      "category": "non-functional",
      "priority": 2,
      "bootstrap": false,
      "description": "{{e.g.: All new UI uses Luma-styled shadcn components — verified per shadcn-luma skill checklist}}",
      "steps": ["{{...}}"],
      "passes": false
    }
  ]
}
